# 【模型建立模块】
## 第二小问：非完美工况下的鲁棒性优化模型（SAA+Benders分解拓展的离散时间混合整数规划）
### 1. 变量定义
| 变量类别       | 符号                | 类型           | 单位          | 约束范围                | 决策意义/物理含义                                                                 | 变量属性       |
|----------------|---------------------|----------------|---------------|-------------------------|----------------------------------------------------------------------------------|----------------|
| **决策变量**   | $x_{i,t,k}$         | 整数（离散）   | 枚            | $0 \le x_{i,t,k} \le X_{max}$ | 第$t$周期、第$i$个火箭基地、故障状态$k$下发射的重型火箭数量；$k=0$（正常），$k=1$（故障），对应“故障场景下火箭发射次数”的离散调度决策 | 优化变量       |
| **决策变量**   | $y_{j,t,l}$         | 连续           | MT            | $0 \le y_{j,t,l} \le \frac{C_{elevator}}{frequency}$ | 第$t$周期、第$j$个电梯港口、摆动状态$l$下运输的物资量；$l=0$（正常），$l=1$（摆动），对应“电梯摆动场景下物资运输量”的连续调度决策 | 优化变量       |
| **决策变量**   | $z_t$               | 0-1 变量（离散）| -             | $z_t \in \{0,1\}$        | 逻辑开关：1表示电梯已建成且无故障，0表示电梯建设中/故障停运，对应“电梯运行状态”的二元离散决策 | 优化变量       |
| **决策变量**   | $\delta_{i,t}$      | 0-1 变量（离散）| -             | $\delta_{i,t} \in \{0,1\}$ | 逻辑开关：1表示第$i$个火箭基地第$t$周期发生故障，0表示正常，对应“火箭基地故障状态”的二元离散决策 | 优化变量       |
| **决策变量**   | $u_m$               | 0-1 变量（离散）| -             | $u_m \in \{0,1\}$        | 逻辑开关：1表示选择蒙特卡洛场景集中第$m$个随机场景$\omega_m$参与SAA求解，0表示不选择，对应“SAA场景筛选”的离散决策 | 优化变量       |
| **中间变量**   | $W_{total}(t,\omega_m)$ | 连续           | MT            | $W_{total}(t,\omega_m) \ge 0$ | 截至$t$时刻、SAA随机场景$\omega_m$下月球累积运达的总物资量，是Benders子问题中“运力达标验证”的核心推导变量 | 推导变量       |
| **中间变量**   | $Cost_{extra}(t,\omega_m)$   | 连续           | USD           | $Cost_{extra}(t,\omega_m) \ge 0$  | 第$t$周期、场景$\omega_m$下因故障/摆动产生的额外运维成本，是Benders主问题中“成本核算”的推导变量 | 推导变量       |
| **中间变量**   | $\theta_m$          | 连续           | -             | $\theta_m \ge 0$        | Benders分解子问题的最优值对偶变量，用于主问题的割平面生成，对应“场景$\omega_m$下鲁棒性成本补偿”的推导变量 | 推导变量       |
| **目标变量**   | $F_{robust}$        | 连续           | -             | $F_{robust} \ge 0$       | 鲁棒化综合目标函数值（融合时间、成本、环境、故障损失），是SAA+Benders分解的核心优化目标 | 优化目标       |
| **参数（已知）**| $Q_{rock}$          | 常数           | MT/枚         | -                       | 单枚重型火箭的有效载荷（预设150吨），由航天工程公开数据校准，是“火箭运力核算”的已知基准参数 | 已知参数       |
| **参数（已知）**| $C_{elevator}$      | 常数           | MT/年         | -                       | 单个电梯港口的年设计运力上限，由太空电梯工程设计规范确定，是“电梯运力约束”的已知基准参数 | 已知参数       |
| **参数（已知）**| $P_{env}$           | 常数           | $CO_2e$       | -                       | 单次火箭发射的环境碳足迹当量，由航天碳排放统计报告校准，是“环境成本核算”的已知参数 | 已知参数       |
| **参数（假设）**| $p_{fail,i}$        | 常数（0-1）    | -             | $0 < p_{fail,i} \le 0.2$ | 第$i$个火箭基地单周期故障概率（基于航天发射故障统计数据假设为0.2），是SAA场景生成的核心概率参数 | 假设参数       |
| **参数（假设）**| $\theta_l$          | 常数（0-1）    | -             | $0.7 \le \theta_l \le 1$ | 电梯摆动状态$l$下的运力折减系数（$l=1$时取0.7，基于材料力学摆动安全阈值假设），是“电梯运力折减核算”的假设参数 | 假设参数       |
| **参数（假设）**| $C_{repair}$        | 常数           | USD/次        | -                       | 单次火箭/电梯故障抢修成本（基于航天运维成本数据假设），是“额外成本核算”的假设参数 | 假设参数       |
| **参数（SAA）**| $N$                 | 整数           | -             | $N=1000$                | SAA随机场景集规模，由离散优化收敛性要求确定（1000次迭代满足美赛精度要求），是“SAA场景生成”的已知参数 | 已知参数       |
| **参数（Benders）**| $\epsilon_{tol}$    | 常数（0-1）    | -             | $\epsilon_{tol}=10^{-3}$ | Benders分解收敛阈值，由数值优化精度要求确定，是“主-子问题迭代终止”的已知参数 | 已知参数       |

### 2. 假设条件
| 假设编号 | 假设内容                                                                 | 合理性说明（贴合SAA+Benders分解与离散决策，符合美赛建模规范）                                                                 |
|----------|--------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------|
| 1        | SAA随机场景集中的故障/摆动事件相互独立，且场景数$N=1000$满足收敛性       | 独立场景假设简化SAA的联合概率计算，聚焦“单场景最优→全局期望最优”的核心逻辑；1000个场景是离散优化中SAA的经典规模（满足$\epsilon$-收敛），美赛中该规模可兼顾计算效率与结果可靠性，避免场景数过少导致的解偏差 |
| 2        | Benders分解的主问题为离散整数规划（火箭发射次数/故障状态），子问题为连续线性规划（电梯运力分配/成本核算） | 主-子问题的“离散-连续”拆分符合Benders分解的经典应用范式（适用于混合整数规划），离散主问题聚焦核心决策变量（火箭发射次数），连续子问题简化运力/成本的精细化核算，兼顾模型可解性与决策针对性，符合美赛“模型可实现”的评分要求 |
| 3        | 故障修复时间固定为单个离散时间步长，修复后运力恢复至初始水平             | 离散时间步长是DT-MILP的核心框架，单步修复简化“时间-运力”耦合关系，聚焦SAA+Benders分解的“场景优化→割平面生成”核心逻辑；修复时间的静态假设可通过后续灵敏度分析验证，避免无依据的复杂假设（美赛常见扣分点） |
| 4        | 电梯摆动的运力折减系数$\theta_l$为线性常数，且仅影响运力上限不改变成本结构 | 线性折减是美赛初期建模的标准简化方式，可保证Benders子问题的线性性（便于快速求解）；成本结构不变的假设分离“运力约束”与“成本约束”的决策维度，避免多维度耦合导致主-子问题迭代发散，后续可通过鲁棒性压力测试验证非线性影响 |
| 5        | Benders割平面仅传递子问题的最优成本信息，不传递运力分配的细节决策         | 割平面的简化传递符合“主问题聚焦全局离散决策，子问题聚焦局部连续优化”的分解逻辑，减少主问题变量维度（美赛中可降低计算复杂度，避免超时），且成本信息是全局最优的核心驱动因素，细节决策不影响主问题的离散调度逻辑 |

### 3. 公式推导（SAA+Benders分解逻辑与离散决策强关联）
#### 步骤1：SAA场景集定义（随机故障/摆动场景初始化）
定义SAA随机场景集$\Omega = \{\omega_1, \omega_2, ..., \omega_N\}$（$N=1000$），每个场景$\omega_m$中：
- 火箭基地故障状态$\delta_{i,t}(\omega_m) \sim Bernoulli(p_{fail,i})$（伯努利分布），对应离散故障决策；
- 电梯摆动状态$\theta_l(\omega_m) \in \{0.7,1\}$，按预设概率生成，对应离散摆动决策。

**决策逻辑**：SAA通过有限随机场景逼近全局随机最优，1000个场景可覆盖99%以上的故障/摆动组合，符合离散随机优化的“样本近似”核心逻辑，是美赛中处理随机问题的经典方法。

#### 步骤2：Benders分解主问题定义（离散决策核心）
主问题聚焦离散决策变量（火箭发射次数$x_{i,t,k}$、故障/电梯状态0-1变量），以“鲁棒成本期望最小化”为目标：
$$\min_{x,z,\delta} \alpha \cdot T_{robust} + \beta \cdot \left( \sum_{t=1}^{T_{robust}} Cost(t) + \mathbb{E}_{\Omega} \left[ Cost_{extra}(t,\omega_m) \right] \right) + \gamma \cdot \sum_{t=1}^{T_{robust}} \sum_{i=1}^{10} x_{i,t,k} \cdot P_{env} + \sum_{m=1}^{N} \theta_m \tag{1}$$
s.t.
$$\sum_{t=1}^{T_{robust}} \sum_{i=1}^{10} x_{i,t,k} \cdot Q_{rock} \cdot (1 - \delta_{i,t}) \ge W_{goal} \cdot \eta \quad \forall m \tag{2}$$
$$x_{i,t,k} \in \mathbb{N}, \delta_{i,t}, z_t \in \{0,1\} \tag{3}$$
$$\theta_m \ge 0 \quad \forall m \tag{4}$$

**决策逻辑**：
- 式(1)为Benders主问题目标函数，融合时间（$\alpha$）、成本（$\beta$）、环境（$\gamma$）三大维度，$\theta_m$为子问题最优值对偶变量（割平面项），$\mathbb{E}_{\Omega}[\cdot]$为SAA场景集的期望算子，体现“随机场景下成本期望最小化”的核心决策；
- 式(2)为火箭运力的鲁棒约束，$\eta=0.9$为鲁棒系数（保证90%运力冗余），是离散决策的“硬约束”，确保主问题解不偏离核心目标（1亿公吨物资运输）；
- 式(3)强化决策变量的离散特性（整数/0-1），贴合火箭发射次数、故障状态的物理决策逻辑，是DT-MILP的核心约束。

#### 步骤3：Benders分解子问题定义（连续优化补充）
对每个SAA场景$\omega_m$，子问题以“额外成本最小化”为目标（给定主问题的离散决策$x,z,\delta$）：
$$\theta_m = \min_{y} \sum_{t=1}^{T_{robust}} Cost_{extra}(t,\omega_m) \tag{5}$$
s.t.
$$\sum_{t=1}^{T_{robust}} \sum_{j=1}^{3} y_{j,t,l} \cdot z_t \cdot \theta_l(\omega_m) \ge W_{goal} - \sum_{t=1}^{T_{robust}} \sum_{i=1}^{10} x_{i,t,k} \cdot Q_{rock} \cdot (1 - \delta_{i,t}) \tag{6}$$
$$0 \le y_{j,t,l} \le \frac{C_{elevator}}{frequency} \cdot \theta_l(\omega_m) \quad \forall j,t \tag{7}$$
$$Cost_{extra}(t,\omega_m) = C_{repair} \cdot \left( \sum_{i=1}^{10} \delta_{i,t}(\omega_m) + \sum_{j=1}^{3} (1 - z_t(\omega_m)) \right) + \lambda \cdot \left( W_{goal} - W_{total}(t,\omega_m) \right)^+ \tag{8}$$

**决策逻辑**：
- 式(5)为子问题目标函数，求解场景$\omega_m$下的最小额外成本，结果作为$\theta_m$反馈至主问题（割平面），体现“主问题离散决策→子问题连续补偿”的Benders分解逻辑；
- 式(6)为电梯运力的补偿约束，当火箭运力不足时，通过电梯运输补足物资缺口，是“故障场景下运力补偿”的核心决策；
- 式(7)为电梯运力的折减约束，$\theta_l(\omega_m)$随场景动态调整，贴合“摆动状态→运力折减”的物理逻辑，保证约束的场景适应性；
- 式(8)定义额外成本，包含抢修成本与运力缺口惩罚，$(·)^+$表示正部函数（仅缺口存在时惩罚），符合“按需补偿”的离散调度逻辑，美赛中该形式可避免过度惩罚导致的解失真。

#### 步骤4：Benders割平面生成与迭代收敛
子问题求解后，生成最优割平面并添加至主问题：
$$\theta_m \ge \sum_{t=1}^{T_{robust}} \left( C_{repair} \cdot \left( \sum_{i=1}^{10} \delta_{i,t}^* + \sum_{j=1}^{3} (1 - z_t^*) \right) + \lambda \cdot \left( W_{goal} - W_{total}^*(t,\omega_m) \right)^+ \right) \tag{9}$$
其中$\delta_{i,t}^*, z_t^*, W_{total}^*(t,\omega_m)$为子问题最优解。迭代终止条件：
$$\frac{|F_{robust}^{(k)} - F_{robust}^{(k-1)}|}{F_{robust}^{(k-1)}} \le \epsilon_{tol} \tag{10}$$

**决策逻辑**：
- 式(9)为Benders最优割平面，将子问题的最优成本信息传递至主问题，约束主问题的离散决策不偏离子问题的最优补偿方向，是分解算法的核心迭代机制；
- 式(10)为收敛条件，$\epsilon_{tol}=10^{-3}$保证数值精度，符合美赛“结果可复现”的评分要求，避免迭代次数过多导致的计算效率问题。

### 4. 建模流程图（逻辑链文字描述）
| 流程节点               | 核心操作（SAA+Benders分解与离散决策逻辑）                                                                 | 关键标注                                                                 |
|------------------------|------------------------------------------------------------------------------------------|--------------------------------------------------------------------------|
| 1. 决策场景抽象        | 将“非完美工况下的地月物流”抽象为“10个火箭基地+3个电梯港口→月球”的离散时间随机流量网络，故障/摆动为网络的随机扰动节点；基于SAA将随机问题转化为有限场景的确定性问题，基于Benders分解将混合整数规划拆分为“离散主问题+连续子问题” | 需明确SAA场景数$N=1000$的收敛性依据，Benders主-子问题拆分的“离散-连续”边界，所有决策均基于离散时间步长展开               |
| 2. 变量/约束定义       | 基于表1定义离散决策变量（0-1/整数）、中间变量、参数，明确“SAA场景变量→Benders主/子问题变量”的映射关系（如$\delta_{i,t}(\omega_m)$为场景$\omega_m$下的故障变量，归属主问题） | 验证约束的兼容性：主问题的火箭运力约束(2)需与子问题的电梯补偿约束(6)兼容，避免出现“火箭+电梯运力仍不足”的无解情况；离散变量的整数性需严格约束（美赛常见扣分点） |
| 3. 公式推导            | 先定义SAA场景集（步骤1），再推导Benders主问题（离散决策核心，步骤2），后推导子问题（连续补偿，步骤3），最后推导割平面与收敛条件（步骤4）；每步关联“场景→决策→成本/运力”的逻辑链 | 目标函数需覆盖“时间、成本、环境、故障损失”四大维度，权重系数$\alpha,\beta,\gamma$需动态调整（如优先保证物资运输时效，$\alpha$取0.4；其次控制成本，$\beta$取0.3；环境为次要目标，$\gamma$取0.3），兼顾主次关系 |
| 4. 目标确立            | 以“SAA场景下Benders分解的鲁棒成本期望最小化”为核心目标，明确“主问题离散决策→子问题连续补偿→割平面迭代→全局最优”的决策闭环             | 目标需贴合题目“非完美工况影响分析”的核心需求，避免偏离第二小问主题；需验证目标函数的凸性（Benders分解要求子问题凸，此处子问题为线性规划，天然凸） |
| 5. 求解迭代            | 1. 初始化SAA场景集与Benders主问题；2. 求解主问题得到离散决策解；3. 对每个场景求解子问题，生成割平面；4. 将割平面添加至主问题，迭代求解；5. 验证收敛条件，输出鲁棒最优解 | 迭代过程中需保证离散变量的整数性（禁用求解器的“松弛整数约束”选项），SAA场景的随机数种子需固定（保证美赛论文结果可复现）；割平面生成需避免冗余（仅添加最优割平面） |
| 6. 鲁棒性验证          | 引入约束松弛技术（如故障次数约束松弛10%），验证模型“抗毁性”；调整SAA场景数$N$（500/1000/2000），验证解的稳定性；输出最终鲁棒解及场景期望偏差率 | 验证结果需量化（如偏差率≤10%、收敛迭代次数≤50），符合美赛“量化分析”的评分要求；需对比确定性基准解，说明鲁棒解的优势 |