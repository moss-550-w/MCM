# 【模型建立模块】
## 第二小问：非完美工况下的鲁棒性优化模型（离散时间混合整数规划拓展）
### 1. 变量定义
| 变量类别       | 符号                | 类型           | 单位          | 约束范围                | 决策意义/物理含义                                                                 | 变量属性       |
|----------------|---------------------|----------------|---------------|-------------------------|----------------------------------------------------------------------------------|----------------|
| **决策变量**   | $x_{i,t,k}$         | 整数（离散）   | 枚            | $0 \le x_{i,t,k} \le X_{max}$ | 第$t$周期、第$i$个火箭基地、故障状态$k$下发射的重型火箭数量；$k=0$（正常），$k=1$（故障） | 优化变量       |
| **决策变量**   | $y_{j,t,l}$         | 连续           | MT            | $0 \le y_{j,t,l} \le \frac{C_{elevator}}{frequency}$ | 第$t$周期、第$j$个电梯港口、摆动状态$l$下运输的物资量；$l=0$（正常），$l=1$（摆动） | 优化变量       |
| **决策变量**   | $z_t$               | 0-1 变量       | -             | $z_t \in \{0,1\}$        | 逻辑开关：1表示电梯已建成且无故障，0表示电梯建设中/故障停运                          | 优化变量       |
| **决策变量**   | $\delta_{i,t}$      | 0-1 变量       | -             | $\delta_{i,t} \in \{0,1\}$ | 逻辑开关：1表示第$i$个火箭基地第$t$周期发生故障，0表示正常                          | 优化变量       |
| **中间变量**   | $W_{total}(t,\Omega)$ | 连续           | MT            | $W_{total}(t,\Omega) \ge 0$ | 截至$t$时刻、故障场景$\Omega$下月球累积运达的总物资量（$\Omega$为蒙特卡洛随机场景集） | 推导变量       |
| **中间变量**   | $Cost_{extra}(t)$   | 连续           | USD           | $Cost_{extra}(t) \ge 0$  | 第$t$周期因故障/摆动产生的额外运维成本（含抢修、运力补偿成本）                       | 推导变量       |
| **目标变量**   | $F_{robust}$        | 连续           | -             | $F_{robust} \ge 0$       | 鲁棒化综合目标函数值（融合时间、成本、环境、故障损失）                             | 优化目标       |
| **参数（已知）**| $Q_{rock}$          | 常数           | MT/枚         | -                       | 单枚重型火箭的有效载荷（预设150吨）                                               | 已知参数       |
| **参数（已知）**| $C_{elevator}$      | 常数           | MT/年         | -                       | 单个电梯港口的年设计运力上限                                                       | 已知参数       |
| **参数（已知）**| $P_{env}$           | 常数           | $CO_2e$       | -                       | 单次火箭发射的环境碳足迹当量                                                     | 已知参数       |
| **参数（假设）**| $p_{fail,i}$        | 常数（0-1）    | -             | $0 < p_{fail,i} \le 0.2$ | 第$i$个火箭基地单周期故障概率（基于航天发射故障统计数据假设为0.2）                   | 假设参数       |
| **参数（假设）**| $\theta_l$          | 常数（0-1）    | -             | $0.7 \le \theta_l \le 1$ | 电梯摆动状态$l$下的运力折减系数（$l=1$时取0.7，基于材料力学摆动安全阈值假设）       | 假设参数       |
| **参数（假设）**| $C_{repair}$        | 常数           | USD/次        | -                       | 单次火箭/电梯故障抢修成本（基于航天运维成本数据假设）                               | 假设参数       |

### 2. 假设条件
| 假设编号 | 假设内容                                                                 | 合理性说明（贴合离散决策与美赛建模规范）                                                                 |
|----------|--------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------|
| 1        | 故障/摆动事件的发生以离散时间步长为单位，且单周期内最多发生一次故障/摆动 | 符合离散时间混合整数规划（DT-MILP）的核心框架，离散时间步长是模型的基础；单周期单次故障简化了多故障耦合的复杂度，聚焦“运力补偿”的核心决策逻辑，后续可通过蒙特卡洛模拟拓展多故障场景 |
| 2        | 电梯系绳摆动对运力的影响可通过线性折减系数$\theta_l$量化                 | 美赛建模初期的线性简化是行业标准处理方式；系绳摆动的非线性影响可通过鲁棒性压力测试（约束松弛）验证，初期线性假设可保证模型可解性，且材料力学中摆动幅度与运力的关系在安全阈值内近似线性 |
| 3        | 火箭故障为独立伯努利试验，故障概率$p_{fail,i}$不随时间步长变化           | 伯努利试验是蒙特卡洛模拟的基础，可实现随机初始化运行；故障概率静态假设避免了“概率时变”与“离散决策”的双重复杂度，且航天发射故障概率在短期内（建设周期内）无显著波动，符合工程实际 |
| 4        | 故障修复时间固定为单个离散时间步长，修复后运力恢复至初始水平             | 简化离散决策的“时间-运力”耦合关系，聚焦“故障发生→运力损失→补偿发射/运输”的核心逻辑；修复时间的静态假设可通过后续灵敏度分析（调整修复时间步长）验证模型鲁棒性，避免无依据的复杂假设 |
| 5        | 环境碳足迹仅与火箭发射数量相关，故障/摆动不额外增加碳排放               | 分离“运力补偿”与“环境影响”的决策维度，避免多目标耦合导致模型不可解；故障的核心影响是“时间/成本”，环境影响为次要目标，符合美赛“主次目标清晰”的评分要求 |

### 3. 公式推导（离散决策逻辑与数学公式强关联）
#### 步骤1：基准解定义（方案1确定性模型）
先定义无故障/摆动的确定性基准目标函数（对应第一小问的核心公式），作为鲁棒性对比基准：
$$\min F_{det} = \alpha \cdot T + \beta \cdot \sum_{t=1}^{T} Cost(t) + \gamma \cdot \sum_{t=1}^{T} \sum_{i=1}^{10} x_{i,t,0} \cdot P_{env} \tag{1}$$
**决策逻辑**：式(1)为确定性场景下的综合目标函数，聚焦“时间-成本-环境”三目标优化，$\alpha,\beta,\gamma$为动态权重系数，对应10万人月球殖民地建设的核心决策需求。

#### 步骤2：鲁棒化目标函数推导（非完美工况）
引入故障/摆动的额外成本与时间损失，构建鲁棒化目标函数：
$$\min F_{robust} = \alpha \cdot T_{robust} + \beta \cdot \left( \sum_{t=1}^{T_{robust}} Cost(t) + \sum_{t=1}^{T_{robust}} Cost_{extra}(t) \right) + \gamma \cdot \sum_{t=1}^{T_{robust}} \sum_{i=1}^{10} x_{i,t,k} \cdot P_{env} \tag{2}$$
其中，额外成本$Cost_{extra}(t)$的离散决策表达式为：
$$Cost_{extra}(t) = C_{repair} \cdot \left( \sum_{i=1}^{10} \delta_{i,t} + \sum_{j=1}^{3} (1 - z_t) \right) + \lambda \cdot \left( W_{goal} - W_{total}(t,\Omega) \right)^+ \tag{3}$$
**决策逻辑**：
- 式(2)在基准函数基础上，新增故障抢修成本（$C_{repair}$）和运力缺口惩罚成本（$\lambda$为缺口惩罚系数），$T_{robust}$为非完美工况下的总周期，体现“故障→成本增加→周期延长”的离散决策链；
- 式(3)中$(W_{goal} - W_{total}(t,\Omega))^+$表示正的运力缺口（即未达目标的物资量），仅当缺口存在时触发惩罚，符合“按需补偿”的离散调度逻辑；
- 故障状态$\delta_{i,t}$与电梯状态$z_t$的0-1离散特性，直接关联“是否发生故障→是否产生抢修成本”的二元决策。

#### 步骤3：约束方程推导（离散运力红线与逻辑先后）
##### (1) 总量约束（鲁棒化）
$$\sum_{t=1}^{T_{robust}} \left( \sum_{i=1}^{10} x_{i,t,k} \cdot Q_{rock} \cdot (1 - \delta_{i,t}) + \sum_{j=1}^{3} y_{j,t,l} \cdot z_t \cdot \theta_l \right) \ge W_{goal} \tag{4}$$
**决策逻辑**：
- 火箭运力因故障$\delta_{i,t}$折减（故障时$\delta_{i,t}=1$，运力为0），电梯运力因摆动$\theta_l$折减（摆动时$\theta_l=0.7$），体现“故障/摆动→运力下降→需增加发射/运输次数”的离散决策；
- 总量约束需满足1亿公吨的核心目标，是模型的硬约束，确保决策不偏离题目核心需求。

##### (2) 火箭基地故障约束（离散概率）
$$\sum_{t=1}^{T_{robust}} \delta_{i,t} \le \lfloor p_{fail,i} \cdot T_{robust} \rfloor \quad \forall i \tag{5}$$
**决策逻辑**：
- 离散时间步长下，单个火箭基地的故障次数不超过“故障概率×总周期”的整数部分（$\lfloor \cdot \rfloor$为向下取整），符合“故障概率离散化”的决策逻辑；
- 整数约束保证故障次数为离散值，贴合火箭发射“次数为整数”的物理特性。

##### (3) 电梯摆动运力约束
$$0 \le y_{j,t,l} \le \frac{C_{elevator}}{frequency} \cdot \theta_l \quad \forall j,t \tag{6}$$
**决策逻辑**：
- 电梯每周期运力因摆动折减，$\theta_l$的离散取值（0.7/1）直接映射“摆动/正常”的二元状态，约束上限随状态动态调整，符合离散优化的“状态-约束”关联逻辑；
- $frequency$为时间步长频率（如年/季度），保证单位统一，符合美赛公式推导的规范要求。

##### (4) 非负与离散性约束
$$x_{i,t,k} \in \mathbb{N}, \delta_{i,t}, z_t \in \{0,1\}, y_{j,t,l} \ge 0 \quad \forall i,j,t \tag{7}$$
**决策逻辑**：
- 火箭发射数量$x_{i,t,k}$为自然数（离散整数），符合“发射次数为整数”的物理决策；
- 0-1变量$\delta_{i,t}, z_t$的离散性保证故障/电梯状态的二元决策，是DT-MILP模型的核心特征。

#### 步骤4：蒙特卡洛随机验证（全局可靠性）
为验证鲁棒解的全局可靠性，执行以下离散随机迭代：
1. 初始化随机场景集$\Omega = \{\omega_1, \omega_2, ..., \omega_N\}$（$N=1000$次随机运行），每个场景$\omega_m$中，按$p_{fail,i}$随机生成$\delta_{i,t}$，按$\theta_l$随机生成电梯摆动状态；
2. 对每个场景$\omega_m$求解式(2)-(7)，得到场景解$F_{robust}(\omega_m)$；
3. 计算鲁棒解的期望$\mathbb{E}[F_{robust}] = \frac{1}{N} \sum_{m=1}^{N} F_{robust}(\omega_m)$，对比确定性基准解$F_{det}$，验证：
   $$\frac{|\mathbb{E}[F_{robust}] - F_{det}|}{F_{det}} \le \epsilon \tag{8}$$
   （$\epsilon$为可接受偏差，取0.1，即鲁棒解与基准解的偏差不超过10%）
**决策逻辑**：蒙特卡洛的随机初始化运行，覆盖离散故障/摆动的所有可能场景，避免“单点解”的局限性，符合美赛“全局最优”的评分导向。

### 4. 建模流程图（逻辑链文字描述）
| 流程节点               | 核心操作（离散决策逻辑）                                                                 | 关键标注                                                                 |
|------------------------|------------------------------------------------------------------------------------------|--------------------------------------------------------------------------|
| 1. 决策场景抽象        | 将“非完美工况下的地月物流”抽象为“10个火箭基地+3个电梯港口→月球”的离散时间流量网络，故障/摆动为网络的随机扰动节点 | 需明确“离散时间步长”为核心维度，所有决策均基于时间切片展开               |
| 2. 变量/约束定义       | 基于表1定义离散决策变量（0-1/整数）、中间变量、参数，明确“故障→变量→约束”的映射关系（如$\delta_{i,t}→$运力约束） | 验证约束的兼容性：故障次数约束(5)需与总量约束(4)兼容，避免出现无解情况    |
| 3. 公式推导            | 先推导确定性基准公式(1)，再引入故障/摆动的离散状态变量，拓展为鲁棒化公式(2)-(7)，每步关联“状态→成本/运力”的决策逻辑 | 目标函数需覆盖“时间、成本、环境、故障损失”四大维度，权重系数$\alpha,\beta,\gamma$需动态调整以兼顾主次 |
| 4. 目标确立            | 以“鲁棒化综合目标函数最小化”为核心目标，明确“蒙特卡洛验证→全局最优”的决策闭环             | 目标需贴合题目“非完美工况影响分析”的核心需求，避免偏离第二小问主题       |
| 5. 求解迭代            | 1. 初始化蒙特卡洛场景集；2. 对每个场景求解DT-MILP模型；3. 计算期望解并对比基准解；4. 调整惩罚系数$\lambda$迭代优化 | 迭代过程中需保证离散变量的整数性，避免求解器输出非整数解（美赛常见扣分点） |
| 6. 鲁棒性验证          | 引入约束松弛技术（如故障次数约束松弛10%），验证模型“抗毁性”，输出最终鲁棒解               | 验证结果需量化（如偏差率、恢复时间），符合美赛“量化分析”的评分要求       |

### 5. 模型核心特征总结
本模型以离散时间混合整数规划（DT-MILP）为框架，通过0-1离散变量刻画故障/摆动的二元状态，以整数变量刻画火箭发射的离散次数，实现“非完美工况→离散状态→运力约束→成本/时间优化”的全链路数学映射；蒙特卡洛随机模拟的引入，保证了离散决策解的全局可靠性，符合美赛“模型可验证、结果可复现”的核心评分标准。