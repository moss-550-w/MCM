# 【模型建立模块】
## 引言
本模块基于【模型选择模块】确定的**整数规划+约束松弛优化（基础适配型）**与**多目标整数规划+遗传算法+层次分析法（创新融合型）**两类模型，紧扣地月物流的**离散决策逻辑**，完成问题4“评估并最小化环境影响”的数学建模全流程。建模以“离散决策变量（火箭发射数、基地/港口启用状态）”为核心，结合预处理的环境排放、运力、成本等数据，严格遵循美赛论文公式推导规范，实现**离散决策逻辑与数学公式的强关联**，同时兼顾模型的可求解性与场景适配性。

### 核心下标定义（全模块统一）
为保证公式符号规范，先定义建模核心下标（后续公式直接沿用，不再重复说明）：
- $i \in \{1,2,\dots,10\}$：全球10个火箭发射基地的离散编号
- $j \in \{1,2,3\}$：3个银河电梯港口的离散编号
- $t \in \{1,2,\dots,T\}$：离散决策周期（以**年**为单位，$T$为月球殖民地建设总周期，由Q1求解结果确定）
- $s \in \{1,2,3\}$：殖民地建设阶段（1-基建期，2-运营期，3-稳定期）
- $k \in \{1,2\}$：运输模式（1-火箭运输，2-电梯运输）

## 1 变量定义
按**决策变量、中间变量、目标变量、参数**四类划分，明确变量类型、单位、约束范围及决策意义，同时区分**已知参数（预处理数据/题目给定）、假设参数（基于行业数据校准）、优化变量（模型求解得到）**，所有离散变量均标注离散特征（整数/0-1），贴合离散决策逻辑。

### 1.1 决策变量（优化变量，模型核心求解对象）
| 变量符号 | 变量类型 | 单位 | 约束范围 | 决策意义 |
|----------|----------|------|----------|----------|
| $x_{i,t}$ | 非负整数离散变量 | 枚 | $x_{i,t} \leq X_{i,\text{max}}$ | 基础模型：第$t$周期第$i$个火箭基地的火箭发射数量，对应“火箭运输”的核心离散决策 |
| $y_{j,t}$ | 非负连续变量 | 万公吨 | $y_{j,t} \leq Y_{j,\text{max}}$ | 基础模型：第$t$周期第$j$个电梯港口的物资运输量，为电梯运力的连续决策变量 |
| $x_{i,t,s}$ | 非负整数离散变量 | 枚 | $x_{i,t,s} \leq X_{i,s,\text{max}}$ | 创新模型：第$s$阶段第$t$周期第$i$个火箭基地的火箭发射数量，加入阶段化离散决策维度 |
| $y_{j,t,s}$ | 非负连续变量 | 万公吨 | $y_{j,t,s} \leq Y_{j,s,\text{max}}$ | 创新模型：第$s$阶段第$t$周期第$j$个电梯港口的物资运输量，加入阶段化连续决策维度 |
| $z_{i}$ | 0-1离散变量 | - | $z_i \in \{0,1\}$ | 创新模型：$z_i=1$表示启用第$i$个火箭基地，$z_i=0$表示不启用，对应基地选址的离散决策 |
| $\sigma_t$ | 非负连续松弛变量 | 吨CO₂当量 | $\sigma_t \geq 0$ | 基础模型：第$t$周期碳排放约束的松弛量，用于处理“碳排放上限”与“运输时效”的硬约束冲突 |

### 1.2 中间变量（由决策变量推导，辅助公式构建）
| 变量符号 | 变量类型 | 单位 | 约束范围 | 决策意义 |
|----------|----------|------|----------|----------|
| $E_{1,i,t}$ | 非负连续变量 | 吨CO₂当量 | $E_{1,i,t} \geq 0$ | 第$t$周期第$i$个火箭基地发射$x_{i,t}$枚火箭的碳排放总量，由火箭单枚排放系数推导 |
| $E_{2,j,t}$ | 非负连续变量 | 吨CO₂当量 | $E_{2,j,t} \geq 0$ | 第$t$周期第$j$个电梯港口运输$y_{j,t}$万吨物资的碳足迹总量，由电梯单位运输碳足迹推导 |
| $E_{t}^{\text{total}}$ | 非负连续变量 | 吨CO₂当量 | $E_{t}^{\text{total}} \geq 0$ | 第$t$周期所有运输模式的环境影响总量（碳足迹当量），为目标函数核心中间变量 |
| $Q_{t}^{\text{total}}$ | 非负连续变量 | 万公吨 | $Q_{t}^{\text{total}} \geq 0$ | 第$t$周期所有运输模式的物资运输总量，用于验证总量约束满足性 |
| $\omega_{s,k}$ | 非负连续变量 | - | $0 \leq \omega_{s,k} \leq 1, \sum \omega_{s,k}=1$ | 创新模型：第$s$阶段第$k$类目标的AHP动态权重，由层次分析法计算得到 |

### 1.3 目标变量（模型优化的核心对象，由中间/决策变量推导）
| 变量符号 | 变量类型 | 单位 | 约束范围 | 决策意义 |
|----------|----------|------|----------|----------|
| $F_1$ | 非负连续变量 | 吨CO₂当量 | $F_1 \geq 0$ | 基础模型目标变量：全周期总环境影响（碳足迹当量），需实现**最小化** |
| $F_2$ | 非负连续变量 | 万美元 | $F_2 \geq 0$ | 创新模型目标变量：全周期总运输成本，需实现**最小化** |
| $F_3$ | 非负连续变量 | 年 | $F_3 \geq 0$ | 创新模型目标变量：完成总物资运输的总时间，需实现**最小化** |
| $F_{\text{mix}}$ | 非负连续变量 | 综合单位 | $F_{\text{mix}} \geq 0$ | 创新模型目标变量：多目标加权综合值，需实现**最小化** |

### 1.4 参数（已知/假设，建模固定输入）
| 变量符号 | 参数属性 | 单位 | 数值来源 | 物理意义 |
|----------|----------|------|----------|----------|
| $e_1$ | 已知参数 | 吨CO₂当量/枚 | 预处理火箭排放数据 | 单枚火箭发射的碳排放系数（取100-150公吨载荷均值对应的排放） |
| $e_2$ | 已知参数 | 吨CO₂当量/万公吨 | 预处理电梯碳足迹数据 | 电梯运输1万公吨物资的全生命周期碳足迹系数（建设+运营） |
| $X_{i,\text{max}}$ | 已知参数 | 枚/年 | 题目给定火箭基地数据 | 第$i$个火箭基地的年最大发射能力（离散运力约束） |
| $Y_{j,\text{max}}$ | 已知参数 | 万公吨/年 | 题目给定：17.9万公吨/港/年 | 第$j$个电梯港口的年最大运输能力 |
| $E_t^{\text{limit}}$ | 已知参数 | 吨CO₂当量/年 | 预处理环境数据 | 第$t$周期地球环境碳排放上限（硬约束阈值） |
| $Q_{\text{total}}$ | 已知参数 | 万公吨 | 题目给定：1亿公吨 | 月球殖民地建设总物资需求（10000万公吨） |
| $C_1$ | 假设参数 | 万美元/枚 | Falcon Heavy 2026成本预测 | 单枚火箭的单位运输成本 |
| $C_2$ | 假设参数 | 万美元/万公吨 | 太空电梯边际成本假设 | 电梯运输1万公吨物资的单位运输成本 |
| $M$ | 假设参数 | 吨CO₂当量/万美元 | 环境经济核算数据 | 碳排放松弛的单位惩罚系数（超额1吨排放的经济惩罚） |
| $T_s$ | 假设参数 | 年 | 殖民地建设规划 | 第$s$阶段的持续周期（如基建期$T_1=5$年） |

## 2 假设条件
结合**离散决策简化需求**与B题“地月大规模物流调度”的场景特点，提出5个核心假设，每个假设均说明合理性与场景适配性，避免无依据假设（美赛常见扣分点），非完美工况、突发因素等均通过后续鲁棒性分析覆盖，不影响基础建模逻辑。

### 假设1：运输决策周期按**年**离散划分，且单周期内火箭发射数、电梯运输量为定值
**合理性**：月球殖民地建设为长期工程（1亿公吨物资运输），以“年”为离散决策周期，既符合运力数据（电梯单港年运力、火箭基地年发射能力）的统计维度，又能大幅降低离散决策的变量维度，避免模型求解复杂度过高；单周期内定值假设为基础建模简化，后续可通过周/月级细化迭代修正，且不偏离核心优化目标。

### 假设2：环境影响可统一量化为**碳足迹当量（吨CO₂当量）**，火箭碳排放、电梯碳足迹的量化方法为固定系数
**合理性**：问题4的核心是“最小化环境影响”，需将多源环境影响（火箭高层大气碳排放、电梯建设工业排放、运营电力排放）统一量化维度；碳足迹当量为国际通用的环境影响量化指标，预处理数据已通过行业标准将各类排放转化为该指标，固定系数假设为基础建模基准，阶段化波动可通过权重调整覆盖。

### 3：火箭单枚碳排放系数$e_1$、电梯单位运输碳足迹系数$e_2$为定值，忽略载荷波动（100-150公吨）的微小影响
**合理性**：题目中火箭单次载荷为100-150公吨，其对应的碳排放波动在±10%以内，属于微小扰动，基础建模中取均值系数可简化离散决策逻辑；载荷波动的影响已在Q2的随机模拟与鲁棒性分析中覆盖，无需在问题4的环境优化基础模型中重复考虑。

### 假设4：火箭基地、电梯港口的运力约束为**硬约束**，不可突破；碳排放约束为**可松弛硬约束**，允许少量超额并施加经济惩罚
**合理性**：火箭基地的发射台数量、电梯系绳的承载能力均由工程设计决定，属于不可突破的物理硬约束，符合实际工程场景；碳排放上限为环境政策硬约束，但实际决策中存在“工程进度优先于严格环保”的权衡需求，通过松弛变量+惩罚系数处理，既贴合实际决策逻辑，又保证模型无不可行解。

### 假设5：月球殖民地10万人的水资源补给需求为**周期刚性值**，并纳入物资运输总量约束，忽略短期波动
**合理性**：问题3已完成水资源补给需求的稳态物流预测，得到人均耗水量与周期补给总量，将其纳入总物资约束，可保证模型的完整性；水资源短期波动为随机因素，已在Q2的鲁棒性分析中考虑，离散决策中取刚性值可简化约束体系，聚焦环境优化核心目标。

## 3 公式推导
分**方案1（基础适配型：整数规划+约束松弛优化）**与**方案2（创新融合型：多目标整数规划+遗传算法+AHP）**两部分推导，每部分先推导**目标函数**，再推导**约束方程**，每步标注**离散决策逻辑**与**公式编号**，变量符号、下标严格遵循前文定义，推导过程贴合离散优化逻辑，符合美赛论文公式推导规范。

### 3.1 方案1：整数规划+约束松弛优化（基础适配型）
**核心逻辑**：以**最小化全周期总环境影响**为单一核心目标，引入松弛变量处理碳排放硬约束冲突，通过整数规划刻画火箭发射数的离散决策，实现“环境最小化”与“决策落地性”的平衡，为美赛保分核心模型。

#### 3.1.1 中间变量推导（为目标函数/约束方程铺垫）
基于离散决策变量与已知参数，先推导环境影响、运输总量的中间变量，建立决策变量与目标/约束的量化关联：
- 式(1)：第$t$周期第$i$个火箭基地的碳排放总量
  $$E_{1,i,t} = e_1 \cdot x_{i,t} \tag{1}$$
  **决策逻辑**：火箭碳排放与发射数量呈线性关系，$x_{i,t}$为整数离散变量，直接刻画火箭发射的离散决策对环境的影响，$e_1$为预处理的固定排放系数。
- 式(2)：第$t$周期第$j$个电梯港口的碳足迹总量
  $$E_{2,j,t} = e_2 \cdot y_{j,t} \tag{2}$$
  **决策逻辑**：电梯碳足迹与运输量呈线性关系，$y_{j,t}$为连续变量，贴合电梯运力的连续调度特征。
- 式(3)：第$t$周期总环境影响（碳足迹当量）
  $$E_{t}^{\text{total}} = \sum_{i=1}^{10} E_{1,i,t} + \sum_{j=1}^{3} E_{2,j,t} \tag{3}$$
  **决策逻辑**：总环境影响为所有火箭基地、电梯港口的环境影响之和，实现多源环境影响的统一量化。
- 式(4)：第$t$周期总物资运输量
  $$Q_{t}^{\text{total}} = 0.01 \cdot \sum_{i=1}^{10} x_{i,t} \cdot \bar{q}_1 + \sum_{j=1}^{3} y_{j,t} \tag{4}$$
  **决策逻辑**：火箭单枚平均载荷$\bar{q}_1=0.015$万公吨（100-150公吨均值），需转换单位与电梯运输量统一；$x_{i,t}$为整数，决定火箭运输的离散物资增量。

#### 3.1.2 目标函数推导
以**最小化全周期总环境影响+碳排放松弛惩罚**为核心目标，构建整数规划的目标函数：
$$\min F_1 = \sum_{t=1}^{T} E_{t}^{\text{total}} + M \cdot \sum_{t=1}^{T} \sigma_t \tag{5}$$
**决策逻辑**：
1. 核心项$\sum_{t=1}^{T} E_{t}^{\text{total}}$为全周期总碳足迹，实现“环境影响最小化”的核心目标；
2. 惩罚项$M \cdot \sum_{t=1}^{T} \sigma_t$为碳排放松弛的经济惩罚，$M$为假设的单位惩罚系数，$\sigma_t$为非负松弛变量，若碳排放未超额则$\sigma_t=0$，惩罚项为0；若超额则惩罚项随超额量线性增加，避免过度松弛；
3. 目标函数中$x_{i,t}$为整数离散变量，$y_{j,t}$、$\sigma_t$为连续变量，构成**混合整数线性规划**，贴合本题“离散-连续”混合决策的特征。

#### 3.1.3 约束方程推导
覆盖**总量约束、运力约束、碳排放约束（含松弛）、非负离散约束**四大类核心约束，所有约束均贴合题目给定条件与离散决策逻辑：
1. **总物资需求约束**（硬约束）
   $$\sum_{t=1}^{T} Q_{t}^{\text{total}} \geq Q_{\text{total}} \tag{6}$$
   **决策逻辑**：全周期运输总量需满足1亿公吨（10000万公吨）的建设需求，“≥”表示允许少量超额，贴合实际物流调度的余量设计。
2. **火箭基地运力约束**（硬约束，离散）
   $$x_{i,t} \leq X_{i,\text{max}}, \quad \forall i,t \tag{7}$$
   **决策逻辑**：每个火箭基地的年发射数量不超过其工程设计的最大能力，$x_{i,t}$为整数离散变量，直接刻画火箭发射的离散运力限制。
3. **电梯港口运力约束**（硬约束，连续）
   $$y_{j,t} \leq Y_{j,\text{max}} = 17.9, \quad \forall j,t \tag{8}$$
   **决策逻辑**：题目给定电梯单港年运力为17.9万公吨，为固定硬约束，贴合电梯运力的工程设计值。
4. **碳排放约束（含松弛，可松弛硬约束）**
   $$E_{t}^{\text{total}} - \sigma_t \leq E_t^{\text{limit}}, \quad \forall t \tag{9}$$
   **决策逻辑**：核心约束改进点——引入松弛变量$\sigma_t$，将原不可松弛的碳排放硬约束转化为“可松弛约束”；当$E_{t}^{\text{total}} \leq E_t^{\text{limit}}$时，$\sigma_t=0$，约束退化为传统硬约束；当$E_{t}^{\text{total}} > E_t^{\text{limit}}$时，$\sigma_t=E_{t}^{\text{total}} - E_t^{\text{limit}}$，允许少量超额并通过目标函数的惩罚项实现权衡。
5. **非负与离散约束**（模型固有约束）
   $$\begin{cases} x_{i,t} \in N, \quad \forall i,t \\ y_{j,t} \geq 0, \quad \forall j,t \\ \sigma_t \geq 0, \quad \forall t \end{cases} \tag{10}$$
   **决策逻辑**：$x_{i,t} \in N$为**整数离散约束**，保证火箭发射数为正整数（贴合实际决策，无法发射0.5枚火箭）；$y_{j,t}$、$\sigma_t$为非负连续约束，保证运输量与松弛量的物理意义。

### 3.2 方案2：多目标整数规划+遗传算法+AHP（创新融合型）
**核心逻辑**：以**多目标整数规划**为基础框架，结合AHP确定**阶段化动态权重**，刻画“环境影响、成本、时间”三个冲突目标的平衡；引入遗传算法解决高维度离散变量的求解效率问题；加入0-1离散变量刻画火箭基地启用的选址决策，实现“模型定框架、策略提价值”，为美赛冲奖关键模型。

#### 3.2.1 前置：AHP阶段化动态权重计算
先通过层次分析法（AHP）计算各建设阶段$s$的目标权重$\omega_{s,1}$（环境）、$\omega_{s,2}$（成本）、$\omega_{s,3}$（时间），满足$\omega_{s,1}+\omega_{s,2}+\omega_{s,3}=1$，且权重随阶段动态调整（如基建期$\omega_{s,3}$最大，稳定期$\omega_{s,1}$最大），权重结果为多目标函数的定量输入，将定性决策偏好转化为定量系数。

#### 3.2.2 中间变量推导
在方案1的基础上，加入**阶段化、成本、时间**维度的中间变量，适配多目标优化需求：
- 式(11)：第$s$阶段第$t$周期总环境影响
  $$E_{t,s}^{\text{total}} = \sum_{i=1}^{10} e_1 \cdot x_{i,t,s} + \sum_{j=1}^{3} e_2 \cdot y_{j,t,s} \tag{11}$$
- 式(12)：第$s$阶段第$t$周期总运输成本
  $$C_{t,s}^{\text{total}} = \sum_{i=1}^{10} C_1 \cdot x_{i,t,s} \cdot z_i + \sum_{j=1}^{3} C_2 \cdot y_{j,t,s} \tag{12}$$
  **决策逻辑**：加入0-1离散变量$z_i$，仅当启用第$i$个火箭基地（$z_i=1$）时，才计算其运输成本，刻画基地选址的离散决策。
- 式(13)：第$s$阶段完成的物资运输量
  $$Q_{s}^{\text{total}} = \sum_{t=1}^{T_s} \left( 0.015 \cdot \sum_{i=1}^{10} x_{i,t,s} + \sum_{j=1}^{3} y_{j,t,s} \right) \tag{13}$$

#### 3.2.3 目标函数推导
构建**加权多目标最小化函数**，将“环境、成本、时间”三个冲突目标统一为单一综合目标，权重为AHP阶段化动态系数，核心公式：
$$\min F_{\text{mix}} = \sum_{s=1}^{3} \left( \omega_{s,1} \cdot \frac{F_{1s}}{F_{1s}^{\text{max}}} + \omega_{s,2} \cdot \frac{F_{2s}}{F_{2s}^{\text{max}}} + \omega_{s,3} \cdot \frac{F_{3s}}{F_{3s}^{\text{max}}} \right) \tag{14}$$
**决策逻辑**：
1. **归一化处理**：$\frac{F_{1s}}{F_{1s}^{\text{max}}}$、$\frac{F_{2s}}{F_{2s}^{\text{max}}}$、$\frac{F_{3s}}{F_{3s}^{\text{max}}}$为各阶段目标的归一化值，消除量纲差异（环境：吨CO₂，成本：万美元，时间：年），保证权重的有效性；
2. **阶段化加权**：权重$\omega_{s,k}$随建设阶段$s$动态调整，贴合“基建期时间优先、运营期成本优先、稳定期环境优先”的实际决策逻辑；
3. **离散决策融合**：目标函数中$x_{i,t,s}$（整数）、$z_i$（0-1）为离散变量，刻画火箭发射、基地选址的双重离散决策，$y_{j,t,s}$为连续变量，实现混合离散优化；
4. **多目标平衡**：通过加权将冲突目标转化为单一目标，便于遗传算法求解，同时保留各目标的相对重要性，符合多目标离散优化的核心逻辑。

其中，$F_{1s}$、$F_{2s}$、$F_{3s}$为第$s$阶段的单项目标值：
- $F_{1s} = \sum_{t=1}^{T_s} E_{t,s}^{\text{total}}$（第$s$阶段总环境影响）
- $F_{2s} = \sum_{t=1}^{T_s} C_{t,s}^{\text{total}}$（第$s$阶段总运输成本）
- $F_{3s} = T_s$（第$s$阶段的持续时间，由运输量与运力决定）
- $F_{1s}^{\text{max}}$、$F_{2s}^{\text{max}}$、$F_{3s}^{\text{max}}$为各阶段单项目标的最大值（由预处理数据确定）。

#### 3.2.4 约束方程推导
在方案1的约束基础上，加入**阶段化约束、基地启用约束、多目标联动约束**，覆盖更复杂的离散决策场景，所有约束均为硬约束：
1. **阶段化总物资约束**
   $$\sum_{s=1}^{3} Q_{s}^{\text{total}} \geq Q_{\text{total}}, \quad Q_{s}^{\text{total}} \geq Q_{s}^{\text{require}} \tag{15}$$
   **决策逻辑**：总物资满足建设需求，且各阶段满足阶段性物资需求（如基建期需完成40%总物资），贴合分阶段建设的离散决策特征。
2. **阶段化运力约束**
   $$x_{i,t,s} \leq X_{i,s,\text{max}} \cdot z_i, \quad y_{j,t,s} \leq Y_{j,s,\text{max}}, \quad \forall i,j,t,s \tag{16}$$
   **决策逻辑**：火箭基地的阶段运力受启用状态$z_i$（0-1离散变量）限制，未启用的基地（$z_i=0$）发射数为0，强化基地选址的离散决策逻辑。
3. **碳排放严格约束（无松弛）**
   $$E_{t,s}^{\text{total}} \leq E_{t,s}^{\text{limit}}, \quad \forall t,s \tag{17}$$
   **决策逻辑**：创新模型中稳定期碳排放约束为严格硬约束，无松弛，贴合“后期环境优先”的决策重心，基建期/运营期可通过权重调整实现与其他目标的权衡。
4. **基地启用数量约束**
   $$3 \leq \sum_{i=1}^{10} z_i \leq 7 \tag{18}$$
   **决策逻辑**：结合环境与效率需求，限制启用的火箭基地数量为3-7个，$z_i$为0-1离散变量，实现基地选址的离散优化，避免过度启用高污染基地。
5. **非负与离散约束**
   $$\begin{cases} x_{i,t,s} \in N, \quad z_i \in \{0,1\}, \quad \forall i,t,s \\ y_{j,t,s} \geq 0, \quad \forall j,t,s \end{cases} \tag{19}$$
   **决策逻辑**：保留核心离散约束，保证火箭发射数、基地启用状态的物理意义，符合实际决策场景。

## 4 建模流程图
以文字描述**建模全流程的逻辑链**，覆盖「决策场景抽象→变量/约束定义→公式推导→目标确立→求解迭代」核心环节，标注**关键决策节点**与**输出物**，明确各步骤的逻辑关联与判断标准，便于直接转化为美赛论文中的建模流程图（Visio/Mathtype绘制）。

### 4.1 通用建模流程（方案1+方案2均适用）
本流程为地月物流环境优化的离散决策建模通用逻辑，方案1与方案2仅在**目标确立、求解方法、约束处理**环节存在差异，已标注差异化节点。

| 步骤序号 | 核心操作 | 关键决策节点/判断标准 | 差异化处理（方案1/方案2） | 输出物 |
|----------|----------|----------------------|--------------------------|--------|
| 1 | 决策场景抽象 | - | 均抽象为“10火箭基地+3电梯港口→月球”的离散时间流网络；方案2额外按基建/运营/稳定期做阶段化分层 | 场景抽象图、阶段划分表（方案2） |
| 2 | 变量/约束定义 | 验证变量与约束的**逻辑匹配性**（如离散变量对应离散决策） | 方案1：仅定义周期级变量；方案2：增加阶段化变量、0-1基地选址变量 | 变量定义表、约束清单、下标说明文档 |
| 3 | 假设条件设定与验证 | 验证假设的**合理性与无矛盾性**（如硬约束不冲突） | 均遵循5个核心假设；方案2额外验证阶段化假设的适配性 | 假设条件说明文档、合理性分析报告 |
| 4 | 中间变量推导 | 验证中间变量与**决策变量/参数**的量化关联有效性 | 方案1：仅推导环境、运输量中间变量；方案2：增加成本、时间、阶段化中间变量 | 中间变量推导公式、量化关联矩阵 |
| 5 | 公式推导与目标确立 | 1. 验证公式的**符号统一性**；2. 验证目标函数是否覆盖核心决策需求 | 方案1：单目标（环境最小化）+约束松弛；方案2：多目标（环境/成本/时间）+AHP动态权重 | 目标函数公式、约束方程公式、公式编号文档（美赛规范） |
| 6 | 约束兼容性检验 | 约束是否存在**硬冲突**（如碳排放上限下无法完成运输总量）？<br>→是：执行约束松弛/优先级调整；→否：直接进入求解 | 方案1：引入松弛变量处理冲突；方案2：通过AHP权重调整约束优先级 | 约束兼容性检验报告、松弛变量设计文档（方案1） |
| 7 | 模型求解迭代 | 求解是否**收敛/超时**？<br>→是：输出初始解；→否：简化模型（变量降维/启发式预筛选）重新求解 | 方案1：分支定界法求解混合整数规划；方案2：遗传算法（分层编码+精英保留）求解多目标整数规划 | 初始优化解、求解迭代曲线、收敛性检验报告 |
| 8 | 模型鲁棒性验证 | 解是否满足**鲁棒性要求**（代入Q2故障场景，如20%火箭停飞）？<br>→是：进入方案验证；→否：调整模型参数重新求解 | 均代入Q2非完美工况验证；方案2额外验证阶段化权重的鲁棒性 | 鲁棒性验证报告、抗扰动系数表、解修正建议 |
| 9 | 决策方案验证 | 解是否满足**实际落地性**（如火箭发射数为整数、基地运力不超限）？<br>→是：输出最终方案；→否：返回步骤7重新求解 | 均验证离散决策的落地性；方案2额外验证基地选址的合理性 | 方案验证报告、落地性分析文档 |
| 10 | 最终决策输出 | - | 方案1：输出单目标最优决策方案表；方案2：输出Top3场景化决策方案集（环保/成本/均衡） | 最终决策方案表、优化结果曲线图、帕累托解集（方案2） |

### 4.2 流程核心说明
1. **离散决策逻辑贯穿全程**：从场景的离散时间/阶段划分，到离散变量（整数/0-1）的定义，再到离散约束的设计，均以离散决策为核心，保证模型与实际决策场景的强关联；
2. **美赛适配性设计**：所有步骤均输出标准化文档/图表，符合美赛论文“模型清晰、推导规范、结果可展示”的评审要求；公式编号、变量符号统一，便于论文中交叉引用；
3. **迭代优化逻辑**：设置多轮判断节点（约束兼容性、求解收敛性、鲁棒性、落地性），避免模型出现无解、解不贴合实际等问题，保证模型的可求解性与实用性。

**注**：本建模流程可直接转化为美赛论文中的**建模流程图**，以框图形式呈现，标注各步骤的输入/输出与判断分支，提升论文的可视化效果与逻辑严谨性。